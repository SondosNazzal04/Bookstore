let num1 = prompt('enter the first number');
let num2 = prompt('enter the second number');

alert(num1 + num2);

// the encountered problem was that num1 and num2 were not treated as numbers but as strings
